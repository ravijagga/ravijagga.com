# Ravi Jagga Portfolio

### Running Application
 - Install php-cli for creating inbuilt php webserver (for ubuntu `sudo apt install php-cli`)
 - run this command to start php webserver `php -S localhost:8000`